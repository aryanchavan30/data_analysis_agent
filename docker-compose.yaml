version: "3.9"

services:
  snekbox:
    build:
      context: ./snekbox
      dockerfile: Dockerfile
    ports:
      - "8060:8060"
    volumes:
      - ./data/uploads:/data:ro
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: "2.0"
    restart: unless-stopped

  # Optional: uncomment for horizontal scaling with nginx load balancer
  # snekbox-2:
  #   build:
  #     context: ./snekbox
  #     dockerfile: Dockerfile
  #   volumes:
  #     - ./data/uploads:/data:ro
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 2G
  #         cpus: "2.0"
  #   restart: unless-stopped

  # nginx:
  #   image: nginx:alpine
  #   ports:
  #     - "8060:8060"
  #   volumes:
  #     - ./nginx.conf:/etc/nginx/nginx.conf:ro
  #   depends_on:
  #     - snekbox
  #     - snekbox-2
  #   restart: unless-stopped
